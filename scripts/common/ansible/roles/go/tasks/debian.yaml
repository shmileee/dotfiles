---

# TODO:
# fatal: [localhost]: FAILED! => {"changed": false, "msg": "Failed to update
# apt cache: E:The repository
# 'http://ppa.launchpad.net/longsleep/golang-backports/ubuntu jammy Release'
# does not have a Release file."}

# Switch to: goenv

- name: "add golang apt repository"
  become: true
  ansible.builtin.apt_repository:
    repo: "ppa:longsleep/golang-backports"
    state: present
    update_cache: true

- name: "install packages"
  become: true
  ansible.builtin.apt:
    name: golang-go
    autoclean: true
    autoremove: true
