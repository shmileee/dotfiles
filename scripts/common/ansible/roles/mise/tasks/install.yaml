---
- name: "install tools"
  ansible.builtin.command: >
    mise install
  environment:
    GITHUB_TOKEN: "{{ lookup('env', 'GITHUB_TOKEN') }}"
  register: mise_install_result
  changed_when: >-
    'all tools are installed' not in
    (mise_install_result.stdout + mise_install_result.stderr)
