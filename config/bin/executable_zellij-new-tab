#!/usr/bin/env bash

set -euo pipefail

zellij="$HOME/.local/bin/zellij"
session="${ZELLIJ_SESSION_NAME:-main}"
"$zellij" --session "$session" action new-tab --name fish >/dev/null

sleep 0.1
"$zellij" --session "$session" action switch-mode normal
