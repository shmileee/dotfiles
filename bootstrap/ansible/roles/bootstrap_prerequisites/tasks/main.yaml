---

- name: "bootstrap prerequisites | load platform prerequisites"
  ansible.builtin.include_tasks: "{{ ansible_facts['system'] | lower }}.yaml"

- name: "bootstrap prerequisites | load persistent checkout tasks"
  ansible.builtin.include_tasks: checkout.yaml
