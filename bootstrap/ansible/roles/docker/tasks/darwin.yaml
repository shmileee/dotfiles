---

- name: "install rancher desktop"
  community.general.homebrew_cask:
    name: "rancher"
    state: latest
    accept_external_apps: true
  tags: homebrew
  register: rancher_desktop
  until: rancher_desktop is succeeded
  retries: 3
  delay: 15
